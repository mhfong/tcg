# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

secret*
# Agent skills — kept locally, not pushed to GitHub
.github/skills/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Supabase CLI local cache
supabase/.temp/

# Data folder — never commit; lives only on the local machine.
# Exception: the SNKRDUNK apparel-id mapping is committed so the
# daily GitHub Actions scraper can read it. (Other files in data/ are
# large or generated and stay local.)
data/*
!data/.gitkeep
!data/master_snkrdunk.csv
.github/*

# Local scrape output (created by `python scripts/scrape_snkrdunk.py
# --output <file>.csv` and by daily cron runs). Don't commit.
snkrdunk_history.csv
opcg-*.csv
op06-119-*.csv
op05-119-*.csv
/tmp/snkrdunk_*.csv