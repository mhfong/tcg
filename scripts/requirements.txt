# Only used by scripts/parse_server.py and the scraper modules it loads
# (scripts/scraper.py, scripts/scrape_set.py), and by
# scripts/discover_snkrdunk_apparel_ids.py (Playwright + Pillow for
# the optional image-similarity fallback). The frontend (src/) has its
# own package.json and does not consume this file.
#
# Playwright is required by discover_snkrdunk_apparel_ids.py and any
# future JS-rendering scraper. Install browsers with:
#     pip install playwright && playwright install chromium
#
# Pillow + ImageHash are optional \u2014 the discovery script falls back to
# title-only scoring if imagehash isn't installed.
httpx>=0.27
playwright>=1.40
Pillow>=10.0
ImageHash>=4.3
